# 👨‍💻 دليل المطور | Developer Guide

## 🏗️ بنية المشروع التفصيلية

### 📁 Backend/ - الواجهة الخلفية

```
Backend/
├── server.js              # الخادم الرئيسي
├── package.json           # تبعيات الباك إند
├── database/
│   └── memory-db.js       # قاعدة البيانات في الذاكرة
├── middleware/
│   └── validation.js      # التحقق من صحة البيانات
└── routes/
    ├── admin-api.js       # API الإدارة
    ├── admin.js           # مسارات الإدارة
    ├── auth.js            # المصادقة
    ├── data.js            # البيانات العامة
    ├── login.js           # تسجيل الدخول
    ├── menu.js            # القوائم
    ├── recipe.js          # الوصفات
    ├── register.js        # التسجيل
    └── shared.js          # المسارات المشتركة
```

### 📁 frontend/ - واجهة المستخدم

```
frontend/
├── index.html             # الصفحة الرئيسية
├── admin.html             # لوحة الإدارة
├── manifest.json          # إعدادات PWA
├── assets/
│   └── img/              # الصور والأيقونات
├── css/
│   ├── styles.css        # التنسيقات الرئيسية
│   ├── admin.css         # تنسيقات الإدارة
│   └── components.css    # تنسيقات المكونات
└── js/
    ├── main.js           # الملف الرئيسي
    ├── admin.js          # إدارة لوحة التحكم
    ├── cart.js           # عربة التسوق
    ├── components.js     # المكونات المشتركة
    ├── restaurant-api.js # API المطعم
    ├── notifications.js  # الإشعارات
    ├── app-state.js      # حالة التطبيق
    ├── window.js         # إدارة النوافذ
    └── sw.js             # Service Worker
```

## 🔧 APIs المتاحة

### 🔐 المصادقة

- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/logout` - تسجيل الخروج

### 🍽️ القوائم والوصفات

- `GET /api/menu/today` - قائمة اليوم
- `GET /api/recipes` - جميع الوصفات
- `POST /api/recipes` - إضافة وصفة جديدة
- `PUT /api/recipes/:id` - تحديث وصفة
- `DELETE /api/recipes/:id` - حذف وصفة

### 🛒 الطلبات

- `POST /api/orders` - إنشاء طلب جديد
- `GET /api/orders` - جميع الطلبات
- `GET /api/orders/:id` - طلب محدد

### ⚙️ الإدارة

- `GET /api/admin/stats` - إحصائيات النظام
- `GET /api/admin/users` - جميع المستخدمين
- `POST /api/admin/menu` - إدارة القوائم

## 🚀 أوامر التطوير

```bash
# تثبيت جميع التبعيات
npm run install-all

# تشغيل الخادم التطويري مع إعادة التحميل التلقائي
npm run dev

# تشغيل الباك إند فقط
npm run backend

# تشغيل الإنتاج
npm start

# تنظيف المشروع
npm run clean
```

## 🎨 التخصيص

### الألوان الرئيسية

- الأساسي: `#ff6b35` (برتقالي)
- الثانوي: `#2c3e50` (أزرق داكن)
- النجاح: `#27ae60` (أخضر)
- الخطر: `#e74c3c` (أحمر)

### الخطوط

- الرئيسي: 'Cairo', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif
- الأحادي: 'Consolas', 'Monaco', 'Courier New', monospace

## 🔍 استكشاف الأخطاء

### مشاكل شائعة وحلولها

1. **خطأ في منفذ الاتصال**

   ```bash
   Error: EADDRINUSE: address already in use :::3000
   ```

   **الحل:** تغيير المنفذ في ملف `.env` أو إيقاف التطبيق الآخر

2. **مشكلة في تثبيت التبعيات**

   ```bash
   npm ERR! code ENOENT
   ```

   **الحل:** حذف `node_modules` و `package-lock.json` ثم تشغيل `npm install`

3. **خطأ في قاعدة البيانات**
   ```bash
   Database connection failed
   ```
   **الحل:** التأكد من وجود مجلد `Backend/database/`

## 📝 إرشادات المساهمة

1. **تفرع جديد لكل ميزة**

   ```bash
   git checkout -b feature/new-feature
   ```

2. **رسائل ال コミット واضحة**

   ```bash
   git commit -m "✨ Add new menu management feature"
   ```

3. **اختبار قبل الدفع**
   ```bash
   npm test
   ```

## 📞 الدعم

للحصول على المساعدة أو الإبلاغ عن الأخطاء:

- إنشاء Issue في GitHub
- التواصل مع المطور: Tamam

---

**تم إنشاؤه بـ ❤️ لتسهيل التطوير**
